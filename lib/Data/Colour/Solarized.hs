module Data.Colour.Solarized where

import Data.Colour
import Data.Colour.SRGB

base03 :: (Ord a, Floating a) => Colour a
base03  = sRGB24   0  43  54

base02 :: (Ord a, Floating a) => Colour a
base02  = sRGB24   7  54  66

base01 :: (Ord a, Floating a) => Colour a
base01  = sRGB24  88 110 117

base00 :: (Ord a, Floating a) => Colour a
base00  = sRGB24 101 123 131

base0 :: (Ord a, Floating a) => Colour a
base0   = sRGB24 131 148 150

base1 :: (Ord a, Floating a) => Colour a
base1   = sRGB24 147 161 161

base2 :: (Ord a, Floating a) => Colour a
base2   = sRGB24 238 232 213

base3 :: (Ord a, Floating a) => Colour a
base3   = sRGB24 253 246 227

yellow :: (Ord a, Floating a) => Colour a
yellow  = sRGB24 181 137   0

orange :: (Ord a, Floating a) => Colour a
orange  = sRGB24 203  75  22

red :: (Ord a, Floating a) => Colour a
red     = sRGB24 220  50  47

magenta :: (Ord a, Floating a) => Colour a
magenta = sRGB24 211  54 130

violet :: (Ord a, Floating a) => Colour a
violet  = sRGB24 108 113 196

blue :: (Ord a, Floating a) => Colour a
blue    = sRGB24  38 139 210

cyan :: (Ord a, Floating a) => Colour a
cyan    = sRGB24  42 161 152

green :: (Ord a, Floating a) => Colour a
green   = sRGB24 133 153   0

